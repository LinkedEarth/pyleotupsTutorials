name: pyleotups
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - pytest
      - pandas
      - requests
      - tqdm
      - git+https://github.com/LinkedEarth/pyleotups.git@main
      - git+https://github.com/LinkedEarth/pylipd.git@main
      - pyleoclim
